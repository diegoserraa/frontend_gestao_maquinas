# API REST
VITE_API_URL=http://localhost:3000

# WebSocket de telemetria (monitoramento em tempo real).
# Se vazio, e derivado de VITE_API_URL trocando http->ws / https->wss.
VITE_WS_URL=ws://localhost:3000/ws/telemetria

# Web Push
VITE_VAPID_PUBLIC_KEY=
