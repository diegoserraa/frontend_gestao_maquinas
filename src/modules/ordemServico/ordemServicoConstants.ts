// ID do registro "Técnico Externo" na tabela usuarios (ver migration_tecnico_externo.sql).
// Quando ordem.id_tecnico === ID_TECNICO_EXTERNO, a OS é tratada como execução por parceiro externo.
//
// ⚠️ TROQUE o valor abaixo pelo id retornado no INSERT da migration.
export const ID_TECNICO_EXTERNO = 7; // 👈 AJUSTE AQUI
